import { Box, Typography } from "@mui/material";
import { FC } from "react";

export const OnlineConsultationForm: FC = () => {
    return (
        <Box>
            <Typography variant="h5">Online Consultation</Typography>
        </Box>
    );
};