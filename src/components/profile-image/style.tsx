import { Box, BoxProps, styled } from "@mui/material";

export const ProfileImageUploaderWrapper = styled(Box)<BoxProps>({
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    gap: 2,
    padding: 3,
    width: 250,
});