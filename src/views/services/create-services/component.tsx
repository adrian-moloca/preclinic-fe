import { Box } from "@mui/material";
import { FC } from "react";
import AddServiceForm from "../../../components/add-service-form";

export const AddServices: FC = () => {
    return (
        <Box>
            <AddServiceForm />
        </Box>
    );
};