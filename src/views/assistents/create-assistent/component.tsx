import { Box } from "@mui/material";
import { FC } from "react";
import AddAssistentForm from "../../../components/add-assistsent-form";

export const CreateAssistent: FC = () => {
    return (
        <Box>
            <AddAssistentForm />
        </Box>
    );
};