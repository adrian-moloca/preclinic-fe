import { Box, BoxProps, styled } from "@mui/material";

export const CreatePatientWrapper = styled(Box)<BoxProps>({
    overflow: "hidden",
});

export const TitleWrapper = styled(Box)<BoxProps>({
    display: "flex",
    justifyContent: "flex-start",
    marginBottom: "16px",
});