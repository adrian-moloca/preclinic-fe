import { Box } from "@mui/material";
import { FC } from "react";
import CreateProductForm from "../../../components/add-products-form";

export const CreateProduct: FC = () => {
    return (
        <Box>
            <CreateProductForm />
        </Box>
    );
};