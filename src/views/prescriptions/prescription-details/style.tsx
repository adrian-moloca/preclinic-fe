import { Box, BoxProps, styled } from "@mui/material";

export const PatientInfoWrapper = styled(Box)<BoxProps>({
    display: "flex",
    justifyContent: "center",
    gap: "20px",
});

export const SecondSectionWarpper = styled(Box)<BoxProps>({
    display: "flex",
    justifyContent: "space-between",
});