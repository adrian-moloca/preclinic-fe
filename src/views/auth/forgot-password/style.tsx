import { Box, BoxProps, styled } from "@mui/material";

export const ForgotPasswordWrapper = styled(Box)<BoxProps>({
    display: "flex",
    height: "100vh",
    flexDirection: "column",
    justifyContent: "center",
    alignItems: "center"
});