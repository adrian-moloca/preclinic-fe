import { Box, BoxProps, styled } from "@mui/material";

export const ResetPasswordWrapper = styled(Box)<BoxProps>({
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    height: "100vh",
});