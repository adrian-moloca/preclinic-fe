import { Box } from "@mui/material";
import { FC } from "react";
import { CreateLeavesForm } from "../../../components/create-leaves-form/component";

export const CreateLeave: FC = () => {
    return (
        <Box>
            <CreateLeavesForm />
        </Box>
    );
};